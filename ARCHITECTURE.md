# Architecture
SEC new filing -> Gemini agent -> filing sections -> FinBERT + yfinance ->
historical divergence -> agent attention decision -> grounded analyst note.

The LLM does not calculate the financial signal; Python tools do.
